# Analytics Service

A small microservice that aggregates metrics across the platform.

Endpoints:
- `GET /health` — service health check
- `GET /metrics` — platform metrics for internships, applications, and notifications
